name: CI

on: [push, pull_request]

jobs:
  macos:
    runs-on: macos-latest
    
    steps:
      - uses: actions/checkout@v1
        
      - name: setup
        run: ${GITHUB_WORKSPACE}/bin/init.sh
